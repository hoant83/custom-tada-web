import React from 'react';
import { observer } from 'mobx-react-lite';

const AdminSubPanel = () => {
  return (
    <>
      <div className="sub-panel"></div>
    </>
  );
};

export default observer(AdminSubPanel);
