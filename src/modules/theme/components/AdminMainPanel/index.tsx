import React from 'react';
import { observer } from 'mobx-react-lite';

const AdminMainPanel = () => {
  return (
    <>
      <div className="main-panel">Panel</div>
    </>
  );
};

export default observer(AdminMainPanel);
