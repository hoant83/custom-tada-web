import React from 'react';
import { observer } from 'mobx-react';

const LogoutCustomerPage = () => {
  return (
    <>
      <p>Log out on customer page</p>
    </>
  );
};

export default observer(LogoutCustomerPage);
