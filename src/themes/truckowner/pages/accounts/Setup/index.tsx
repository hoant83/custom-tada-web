import React from 'react';
import { observer } from 'mobx-react';

const AccuntSetupPage = () => {
  return (
    <>
      <p>Account Setup Page</p>
    </>
  );
};

export default observer(AccuntSetupPage);
